import { useLocalObservable } from "mobx-react-lite"
import react,{createContext,useContext,usetState} from "react"

export
    return(
        <LightContext.Provider value = {}>

        </LightContext.Provider>
    )